Added 4 hooks. Additional readme file.
For pre-commit hook to work efficiently I used external library (Gitpython), so it is neccesary to have it installed.
I reworked the regex to suit the task.
I wasn't able to test server side hooks as they should work.
